import React from "react";

const Footer = ({ playing }) => {
    return (
        <footer>
            {/* { playing ? (
                <p>W A S D to move <br /> SPACE to jump</p>
            ) : (
                <p>E to play <br /> SCROLL to stop</p>
            )} */}
        </footer>
    );
};

export default Footer;